namespace MemeBot.Core
{
    internal static class Constants
    {
        /// <summary>
        /// The token for the Discord bot.
        /// </summary>
        internal const string BotToken = "";

        /// <summary>
        /// The prefix that will be used to identify when a command is being run.
        /// </summary>
        internal const string CommandPrefix = "$";
    }
}
